export default function Footer() {
  return (
    <footer className="hidden md:block py-6 text-center text-text-muted text-sm">
      © {new Date().getFullYear()} Msekeli Mkwibiso — Thanks for taking the time
      to visit.
    </footer>
  );
}
